<phone:PhoneApplicationPage
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:maps="clr-namespace:Microsoft.Phone.Controls.Maps;assembly=Microsoft.Phone.Controls.Maps" x:Class="Arosbi.DnDZgZ.UI.BusesPage"
                            mc:Ignorable="d"
                            d:DesignWidth="480"
                            d:DesignHeight="768"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            shell:SystemTray.IsVisible="True"
                            DataContext="{Binding Buses, Source={StaticResource Locator}}">

	<Grid x:Name="LayoutRoot">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Border x:Name="MapView"
			BorderBrush="Black"
			Grid.Row="0">
			<maps:Map x:Name="Map"
				CredentialsProvider="{Binding CredentialsProvider}"
				CopyrightVisibility="Collapsed" LogoVisibility="Collapsed"
				ZoomLevel="{Binding Zoom, Mode=TwoWay}"
				Center="{Binding Center, Mode=TwoWay}">
				
				<maps:Map.Mode>
					<maps:RoadMode />
				</maps:Map.Mode>
				
				<maps:MapItemsControl ItemsSource="{Binding BusStops}">
					<maps:MapItemsControl.ItemTemplate>
						<DataTemplate>
							<maps:Pushpin Style="{StaticResource PushpinStyle}"
										  Location="{Binding Location}" 
										  Background="{StaticResource PushpinBrush}" >
								<Image Source="{Binding Icon}" />
							</maps:Pushpin>
						</DataTemplate>
					</maps:MapItemsControl.ItemTemplate>
				</maps:MapItemsControl>
				
				<Button x:Name="ButtonZoomIn"
                        Style="{StaticResource ButtonZoomInStyle}"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Height="56" Width="56" Margin="8,180,0,0" Command="{Binding ZoomInCommand, Mode=OneWay}" IsEnabled="{Binding ZoomInCommand.CanExecute, Mode=TwoWay}" />

                <Button x:Name="ButtonZoomOut"
                        Style="{StaticResource ButtonZoomOutStyle}"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Height="56" Width="56" Margin="8,260,0,0" Command="{Binding ZoomOutCommand, Mode=OneWay}" IsEnabled="{Binding ZoomOutCommand.CanExecute}" />
				
			</maps:Map>		  
		</Border>
	</Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>